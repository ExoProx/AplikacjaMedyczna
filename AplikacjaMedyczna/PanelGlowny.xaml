<Page
    x:Class="AplikacjaMedyczna.PanelGlowny"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AplikacjaMedyczna"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- Gradientowe tło -->
    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="White" Offset="0" />
            <GradientStop Color="#D6EAF8" Offset="0.7" />
            <GradientStop Color="#85C1E9" Offset="1" />
        </LinearGradientBrush>
    </Page.Background>

    <Grid>
        <!-- SplitView z przyciskami nawigacyjnymi -->
        <SplitView x:Name="splitView" PaneBackground="#004AAD"
           IsPaneOpen="False" OpenPaneLength="124" CompactPaneLength="48" DisplayMode="Inline">
            <SplitView.Pane>
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button x:Name="PatientChoiceButton" Visibility="Collapsed" Content="Wybierz" Click="NavButton_Click" IsEnabled="False" Grid.Row="1" Width="100" Height="50" Style="{StaticResource NavToggleButtons}"/>
                    <Button x:Name="PanelButton" Content="Panel" Click="NavButton_Click" Grid.Row="2" Width="100" Height="50" Style="{StaticResource NavToggleButtons}"/>
                    <Button x:Name="WpisyButton" Content="Wpisy" Click="NavButton_Click" Grid.Row="3" Width="100" Height="50" Style="{StaticResource NavToggleButtons}"/>
                    <Button x:Name="ReceptyButton" Content="Recepty" Click="NavButton_Click" Grid.Row="4" Width="100" Height="50" Style="{StaticResource NavToggleButtons}"/>
                    <Button x:Name="SkierowaniaButton" Content="Skierowania" Click="NavButton_Click" Grid.Row="5" Width="100" Height="50" Style="{StaticResource NavToggleButtons}"/>
                    <Button x:Name="WynikiButton" Visibility="Collapsed" IsEnabled="False" Content="Wyniki" Click="NavButton_Click" Grid.Row="6" Width="100" Height="50" Style="{StaticResource NavToggleButtons}"/>
                    <Button x:Name="Logout" Content="Wyloguj" Click="NavButton_Click" Grid.Row="8" VerticalAlignment="Bottom" Margin="0, 0, 0, 20" Width="100" Height="50" Style="{StaticResource NavToggleButtons}"/>
                </Grid>
            </SplitView.Pane>
            <SplitView.Content>
                <Grid x:Name="ContentGrid" Margin="10" HorizontalAlignment="Center">
                    <!-- Patient Info Panel -->
                    <Border Background="#004AAD" CornerRadius="8" BorderBrush="Black" BorderThickness="2" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="50,50,0,0" Padding="10" Width="350" Height="430">
                        <StackPanel x:Name="PatientInfoPanel" Orientation="Vertical">
                            <TextBlock Text="Informacje Pacjenta" FontWeight="Bold" FontSize="16" Foreground="White" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Imię:" FontWeight="Bold" Foreground="White" Width="140"/>
                                <TextBox x:Name="ImieTextBox" Width="150" IsReadOnly="True"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <TextBlock Text="Nazwisko:" FontWeight="Bold" Foreground="White" Width="140"/>
                                <TextBox x:Name="NazwiskoTextBox" Width="150" IsReadOnly="True"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <TextBlock Text="PESEL:" FontWeight="Bold" Foreground="White" Width="140"/>
                                <TextBox x:Name="PeselTextBox" Width="150" IsReadOnly="True"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <TextBlock Text="Adres Zamieszkania:" FontWeight="Bold" Foreground="White" Width="140"/>
                                <TextBox x:Name="AdresZamieszkaniaTextBox" Width="150" IsReadOnly="True"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <TextBlock Text="Numer Kontaktowy:" FontWeight="Bold" Foreground="White" Width="140"/>
                                <TextBox x:Name="NumerKontaktowyTextBox" Width="150" IsReadOnly="True"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <TextBlock Text="Data Urodzenia:" FontWeight="Bold" Foreground="White" Width="140"/>
                                <TextBox x:Name="DataUrodzeniaTextBox" Width="150" IsReadOnly="True"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <TextBlock Text="Typ Krwi:" FontWeight="Bold" Foreground="White" Width="140"/>
                                <TextBox x:Name="TypKrwiTextBox" Width="150" IsReadOnly="True"/>
                            </StackPanel>
                            <StackPanel x:Name="ObecnyPacjentPanel" Visibility="Collapsed" Orientation="Vertical">
                                <TextBlock Text="Obecny Pacjent" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" TextAlignment="Center" Foreground="White" Margin="-10,10,0,10"/>
                                <ComboBox x:Name="ObecnyPacjentComboBox" Width="300" SelectionChanged="ObecnyPacjentComboBox_SelectionChanged"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <!-- Allergies ListView -->
                    <Border Background="#004AAD" CornerRadius="8" BorderBrush="Black" BorderThickness="2" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="50,500,0,0" Padding="10" Width="350" Height="200">
                        <ScrollViewer VerticalScrollBarVisibility="Hidden">
                            <StackPanel x:Name="AllergiesPanel" Orientation="Vertical">
                                <TextBlock Text="Alergie Pacjenta" FontWeight="Bold" FontSize="16" Foreground="White" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                                <ListView x:Name="AllergiesListView" Background="White">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Alergia}" FontSize="14" Foreground="Black"/>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                    <!-- Additional Panels -->
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="500,50,50,0">
                        <Border Background="#004AAD" CornerRadius="8" BorderBrush="Black" BorderThickness="2" Padding="10" Width="500" Height="150" Margin="0,0,0,10">
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="Wpisy" FontWeight="Bold" FontSize="16" Foreground="White" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="WpisyTextBlock" FontSize="14" Foreground="White" TextWrapping="Wrap" Margin="0,0,0,10" HorizontalAlignment="Left"/>
                            </StackPanel>
                            </Border>
                            <Border Background="#004AAD" CornerRadius="8" BorderBrush="Black" BorderThickness="2" Padding="10" Width="500" Height="146" Margin="0,10,0,10">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="Recepty" FontWeight="Bold" FontSize="16" Foreground="White" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="ReceptyTextBlock" FontSize="14" Foreground="White" Margin="0,0,0,10" HorizontalAlignment="Left"/>

                            </StackPanel>
                        </Border>
                        <Border Background="#004AAD" CornerRadius="8" BorderBrush="Black" BorderThickness="2" Padding="10" Width="500" Height="146" Margin="0,10,0,10">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="Skierowania" FontWeight="Bold" FontSize="16" Foreground="White" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="SkierowaniaTextBlock" HorizontalAlignment="Left" FontSize="14" Foreground="White" TextWrapping="Wrap" Margin="0,0,0,10" />
                            </StackPanel>
                        </Border>
                        <Border Background="#004AAD" CornerRadius="8" BorderBrush="Black" BorderThickness="2" Padding="10" Width="500" Height="146" Margin="0,10,0,0">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="Wyniki" FontWeight="Bold" FontSize="16" Foreground="White" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="WynikiTextBlock" FontSize="14" Foreground="White" Margin="0,0,0,10" HorizontalAlignment="Left"/>

                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </SplitView.Content>
        </SplitView>

        <!-- Przycisk do otwierania/zamykania panelu nawigacyjnego -->
        <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12">
            <ToggleButton x:Name="NavbarToggleButton" Width="40" Height="40" 
                          Checked="NavbarToggleButton_Checked" Unchecked="NavbarToggleButton_Unchecked" 
                          Style="{StaticResource NavToggleButtonStyle}">
                <FontIcon Glyph="&#xE700;" Foreground="White"/>
            </ToggleButton>
        </Grid>
    </Grid>
</Page>


