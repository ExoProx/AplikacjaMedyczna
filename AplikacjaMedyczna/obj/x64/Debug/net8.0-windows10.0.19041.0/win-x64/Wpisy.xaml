<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AplikacjaMedyczna.Wpisy"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AplikacjaMedyczna"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <SplitView x:ConnectionId='2' x:Name="splitView" PaneBackground="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
           IsPaneOpen="False" OpenPaneLength="124" CompactPaneLength="48" DisplayMode="Inline">
            <SplitView.Pane>
                <Grid Margin="12,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <Button x:ConnectionId='5' x:Name="PatientChoiceButton" Visibility="Collapsed" 
                Content="Wybierz"                         IsEnabled="False" Grid.Row="1" Width="100"/>
                    <Button x:ConnectionId='6' x:Name="WpisyButton" Content="Wpisy"                         Grid.Row="2" Width="100"/>
                    <Button x:ConnectionId='7' x:Name="ReceptyButton" Content="Recepty"                         Grid.Row="3" Width="100"/>
                    <Button x:ConnectionId='8' x:Name="SkierowaniaButton" Content="Skierowania"                         Grid.Row="4" Width="100"/>
                    <Button x:ConnectionId='9' x:Name="WynikiButton" Content="Wyniki"                         Grid.Row="5" Width="100"/>
                    <Button x:ConnectionId='10' x:Name="BackButton" Content="Powrót"                         Grid.Row="6" Width="100"/>
                </Grid>
            </SplitView.Pane>
            <ListView x:ConnectionId='11'
    x:Name="FilteredListView"
    ItemsSource="{Binding WpisyCollection}"
    SelectionMode="Single"
    Height="400"
    Width="400"
    BorderThickness="1"
    BorderBrush="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                
    IsItemClickEnabled="True" 
                                          >

                <!-- Define the ItemTemplate -->
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" Padding="10">
                            <TextBlock Text="{Binding wpis}" FontSize="16" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding dataWpisu}" FontSize="14" />
                            <TextBlock Text="{Binding peselPacjenta}" FontSize="14" />
                            <TextBlock Text="{Binding danePersonelu}" FontSize="14" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </SplitView>
        <ContentDialog x:ConnectionId='3'
    x:Name="WpisDetailDialog"
    Title="Wpis Details"
    CloseButtonText="Close"
                                               >
            <StackPanel>
                <TextBlock Text="Wpis:" FontWeight="Bold" />
                <TextBlock Text="{Binding wpis}" Margin="0,0,0,10" />

                <TextBlock Text="Data Wpisu:" FontWeight="Bold" />
                <TextBlock Text="{Binding dataWpisu}" Margin="0,0,0,10" />

                <TextBlock Text="PESEL Pacjenta:" FontWeight="Bold" />
                <TextBlock Text="{Binding peselPacjenta}" Margin="0,0,0,10" />

                <TextBlock Text="Dane personelu:" FontWeight="Bold" />
                <TextBlock Text="{Binding danePersonelu}" />
            </StackPanel>
        </ContentDialog>
        <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12">
            <ToggleButton x:ConnectionId='4' x:Name="NavbarToggleButton"
          Width="40" Height="40"
                                              
                                                  
                      Background="Transparent">
                <FontIcon Glyph="&#xE700;" />
            </ToggleButton>
        </Grid>
    </Grid>
</Page>

