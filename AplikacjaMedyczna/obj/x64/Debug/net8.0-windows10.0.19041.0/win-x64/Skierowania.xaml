<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AplikacjaMedyczna.Skierowania"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AplikacjaMedyczna"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>

        <SplitView x:ConnectionId='2' x:Name="splitView" PaneBackground="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
           IsPaneOpen="False" OpenPaneLength="124" CompactPaneLength="48" DisplayMode="Inline">
            <SplitView.Pane>
                <Grid Margin="12,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <Button x:ConnectionId='5' x:Name="WpisyButton" Content="Wpisy"                         Grid.Row="1" Width="100"/>
                    <Button x:ConnectionId='6' x:Name="ReceptyButton" Content="Recepty"                         Grid.Row="2" Width="100"/>
                    <Button x:ConnectionId='7' x:Name="SkierowaniaButton" Content="Skierowania"                         Grid.Row="3" Width="100"/>
                    <Button x:ConnectionId='8' x:Name="WynikiButton" Content="Wyniki"                         Grid.Row="4" Width="100"/>
                    <Button x:ConnectionId='9' x:Name="BackButton" Content="Powrót"                         Grid.Row="5" Width="100"/>
                </Grid>
            </SplitView.Pane>
            <ListView x:ConnectionId='10'
    x:Name="FilteredListView"
    ItemsSource="{Binding SkierowaniaCollection}"
    SelectionMode="Single"
    Height="400"
    Width="400"
    BorderThickness="1"
    BorderBrush="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                
    IsItemClickEnabled="True" 
                                          >

                <!-- Define the ItemTemplate -->
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" Padding="10">
                            <TextBlock Text="{Binding skierowanie}" FontSize="16" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding dataSkierowania}" FontSize="14" />
                            <TextBlock Text="{Binding peselPacjenta}" FontSize="14" />
                            <TextBlock Text="{Binding danePersonelu}" FontSize="14" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </SplitView>
        <ContentDialog x:ConnectionId='3'
    x:Name="SkierowanieDetailDialog"
    Title="Informacje o skierowaniu"
    CloseButtonText="Close">
            <StackPanel>
                <TextBlock Text="Skierowanie:" FontWeight="Bold" />
                <TextBlock Text="{Binding skierowanie}" Margin="0,0,0,10" />
                
                <TextBlock Text="Data skierowania:" FontWeight="Bold" />
                <TextBlock Text="{Binding dataSkierowania}" Margin="0,0,0,10" />

                <TextBlock Text="PESEL Pacjenta:" FontWeight="Bold" />
                <TextBlock Text="{Binding peselPacjenta}" Margin="0,0,0,10" />

                <TextBlock Text="Dane personelu:" FontWeight="Bold" />
                <TextBlock Text="{Binding danePersonelu}" />
            </StackPanel>
        </ContentDialog>
        <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12">
            <ToggleButton x:ConnectionId='4' x:Name="NavbarToggleButton"
          Width="40" Height="40"
                                              
                                                  
                      Background="Transparent">
                <FontIcon Glyph="&#xE700;" />
            </ToggleButton>
        </Grid>
    </Grid>
</Page>

