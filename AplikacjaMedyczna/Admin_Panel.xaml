<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AplikacjaMedyczna.Admin_Panel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AplikacjaMedyczna"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Page.Resources>
        <Style x:Name="ContentDialogStyle" TargetType="ContentDialog">
            <Setter Property="Background" Value="#F0F8FF" />
            <Setter Property="BorderBrush" Value="#004AAD" />
            <Setter Property="BorderThickness" Value="2" />
        </Style>
    </Page.Resources>

    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="White" Offset="0" />
            <GradientStop Color="#D6EAF8" Offset="0.7" />
            <GradientStop Color="#85C1E9" Offset="1" />
        </LinearGradientBrush>
    </Page.Background>

    <Grid>

        <SplitView x:Name="splitView" PaneBackground="#004AAD"
                   IsPaneOpen="False" OpenPaneLength="180" CompactPaneLength="48" DisplayMode="Inline">
            <SplitView.Pane>
                <Grid Margin="10,60,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="70"/>
                    </Grid.RowDefinitions>
                    <Button x:Name="ManageWorkersButton" Content="Zarządaj Lekarzami" Click="NavButton_Click" Grid.Row="0" Width="150" Height="50"/>
                    <Button x:Name="InsertPatientButton" Content="Dodaj Pacjenta" Click="NavButton_Click" Grid.Row="2" Width="150" Height="50"/>
                    <Button x:Name="Logout" Content="Wyloguj" Click="NavButton_Click" Grid.Row="5" VerticalAlignment="Bottom" Margin="0,0,0,5" Width="150" Height="50"/>
                </Grid>
            </SplitView.Pane>
            <SplitView.Content>
                <Grid Margin="100,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Filter Section -->
                    <Border BorderBrush="Black" BorderThickness="1" Margin="10">
                        <StackPanel Orientation="Horizontal" Margin="10">
                            <TextBox x:Name="FilterNumerId" PlaceholderText="Numer id" Width="150" Margin="5"/>
                            <TextBox x:Name="FilterImie" PlaceholderText="Imię" Width="150" Margin="5"/>
                            <TextBox x:Name="FilterNazwisko" PlaceholderText="Nazwisko" Width="150" Margin="5"/>
                            <TextBox x:Name="FilterProfesja" PlaceholderText="Profesja" Width="150" Margin="5"/>
                            <Button x:Name="FilterButton" Content="Filtruj" Click="FilterButton_Click" Margin="5" Width="100"/>
                        </StackPanel>
                    </Border>

                    <!-- Header Section -->
                    <Border BorderBrush="Black" BorderThickness="1" Margin="10,0,0,0" Grid.Row="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Id" FontWeight="Bold" Margin="28,5,5,5"/>
                            <TextBlock Text="Imię" Grid.Column="1" FontWeight="Bold" Margin="28,5,5,5"/>
                            <TextBlock Text="Nazwisko" Grid.Column="2" FontWeight="Bold" Margin="28,5,5,5"/>
                            <TextBlock Text="Rola" Grid.Column="3" FontWeight="Bold" Margin="28,5,5,5"/>
                            <TextBlock Text="Stan Konta" Grid.Column="4" FontWeight="Bold" Margin="28,5,5,5"/>
                        </Grid>
                    </Border>

                    <!-- DataGrid Section -->
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="10" Grid.Row="2">
                        <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden">
                            <ListView x:Name="MedicalStaffListView" Margin="10">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="200"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Id}" Margin="5"/>
                                            <TextBlock Text="{Binding Imie}" Grid.Column="1" Margin="5"/>
                                            <TextBlock Text="{Binding Nazwisko}" Grid.Column="2" Margin="5"/>
                                            <TextBlock Text="{Binding Rola}" Grid.Column="3" Margin="5"/>
                                            <CheckBox IsChecked="{Binding Aktywne}" Content="Aktywne" Click="CheckBox_Click" Grid.Column="4" Margin="5"/>
                                            <Button Content="Zmień Rolę" Click="EditPersonnelButton_Click" Grid.Column="5" Width="100" Margin="100,5,5,5" Tag="{Binding Id}"/>
                                            <!-- Add Edit button -->

                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </ScrollViewer>
                    </Border>

                    <!-- Add Personnel Button -->
                    <Button x:Name="AddPersonnelButton" Content="Dodaj Personel" Click="AddPersonnelButton_Click" Grid.Row="3" Margin="10" HorizontalAlignment="Right"/>
                </Grid>
            </SplitView.Content>
        </SplitView>
        <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12">
            <ToggleButton x:Name="NavbarToggleButton"
                          Width="40" Height="40"
                          Checked="NavbarToggleButton_Checked"
                          Unchecked="NavbarToggleButton_Unchecked"
                          Background="Transparent">
                <FontIcon Glyph="&#xE700;" />
            </ToggleButton>
        </Grid>
    </Grid>
</Page>
